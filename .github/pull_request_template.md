## What changed

<!-- One or two sentences. What does this PR do, and why? -->

## How it was verified

<!-- Commands actually run and their results, not what CI will do.
     Per the repo's rules: state the numbers, not "tests pass". -->

- [ ] `bun run lint`
- [ ] `bun run test`
- [ ] `bun run test:integration`
- [ ] Ran the affected flow on a device/browser (if UI changed)

## Database changes

- [ ] No schema change
- [ ] Schema changed, and `bun run generate` was run in `packages/db` and the migration committed

## Notes for the reviewer

<!-- Anything non-obvious: a tradeoff taken, a follow-up deliberately left out,
     a TODO.md / issue this relates to. -->
