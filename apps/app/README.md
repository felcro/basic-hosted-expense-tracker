# app

Expo Router client (iOS, Android, web via `react-native-web`), built on `react-native-paper` + `react-native-unistyles`. Talks to the server through a typed Hono RPC client (`src/lib/api.ts`).

Set `EXPO_PUBLIC_API_URL` to point at the server (see `eas.json` per build profile).
